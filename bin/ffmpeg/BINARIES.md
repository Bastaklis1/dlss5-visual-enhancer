# FFmpeg binaries

The executable binaries are intentionally not included in this GitHub copy.

- `bin/ffmpeg.exe` — FFmpeg command-line video/audio processor used by the application.
- `bin/ffprobe.exe` — FFmpeg inspection tool used to read media metadata and stream information.

Restore these files from an official Windows FFmpeg distribution before running the program.
